name: bottom_bar_page_transition
description: To display animation between pages while using bottom bar navigation.
version: 3.0.0
homepage: https://github.com/mdalameen/bottom_bar_page_transition

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
